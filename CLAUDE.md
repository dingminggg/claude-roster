# claude-cockpit

轻量原生办公室面板(PySide6),用来**管理一批真实的 `claude` CLI 控制台窗口**:界面是一张**办公室平面图**——每个员工一个等距工位、按部门分区落在部门区里,谁答完一轮/等你确认就屏幕闪着提醒,点一下把那个黑框弹到眼前。和 desk-buddy(小青蛙)配套(信号通道现已自给自足,见硬约束 4)。

**不是**自绘聊天界面——每个员工就是一个真实独立的 `claude` 控制台(`CREATE_NEW_CONSOLE`)。

## 统一词表(对话 / 文档 / 注释 / 界面文案都照这个来)

| 说 | 不说 |
|---|---|
| **办公室**(这个应用、它那张平面图、它的窗口) | 驾驶舱、面板、画布 |
| **员工**(一个 `claude` 控制台 + 它的工位) | 成员、agent |
| **部门** | — |
| **部门区**(部门在地上那块区域) | 地毯、部门区域 |
| **工位** | 卡片 |

整张界面就是一张办公室平面图,词跟着隐喻走:说「把工位拖到后勤部门区」不用再解释地毯是什么;混着叫读起来像两套东西。**只管人读得到的字**——代码标识符(`Member` / `members` / `member_clicked` / `DeptAreaItem`)保持原样,一次性改标识符的机械 diff 摆不开,hook 和测试还都挂在上面。

## 跑 / 测

```bash
# 启动(无窗后台)
C:\Users\LQ\PhpstormProjects\claude-cockpit\.venv\Scripts\pythonw.exe -m claude_cockpit.main
# 测试(235 个)
QT_QPA_PLATFORM=offscreen .venv/Scripts/python.exe -m pytest -q
# 离屏装配自检(把 QApplication.exec 打桩成返回 0,跑 main() 看 rc 0)。
# ⚠ 跑之前先确认没有 cockpit 在跑:main() 开头的单实例探测发现已有实例会直接
#   return 0,此时 rc 0 的含义是「已有实例」而不是「启动没问题」,自检等于没做
#   (踩过:自检连报三次 rc 0,真机一起就崩)。
# GBK 控制台打印 emoji 要加 PYTHONIOENCODING=utf-8。
# 注意:offscreen 平台没有中文字体,拿它截图看到的全是方框——
# 要肉眼核对画面就别设 QT_QPA_PLATFORM,用默认 windows 平台抓图。
# ⚠ 抓图/试跑的临时脚本**一定要打桩 settings._path**:OfficeWindow.save_layout /
#   closeEvent 会写真的 settings.json,不打桩就把用户摆好的工位坐标全覆盖了
#   (踩过。救法:给正在跑的那个实例发 WM_CLOSE,触发它的 closeEvent 把内存里
#   那份完整布局写回去——强杀进程就没这个机会了)。
```

desk-buddy 通过环境变量 `CLAUDE_COCKPIT_PY` 指向本项目的 pythonw 来联动启动(开机自启 + 小青蛙右键「启动驾驶舱」——那是 desk-buddy 自己的菜单项名,不跟本项目词表走)。

## 源码地图(src/claude_cockpit/)

- **config.py** — `Member` 数据类 + **`OPS_NAME="ops"` / `OPS_DEPT="机房"`**(运维是机房的**固定岗位**:和别人一样是 agents.yaml 里的一条、cwd 自己填,但**名字和部门锁死**——yaml 里写别的部门也会在加载时被扳回机房,面板里「编辑/删除」置灰) + `load_config/save_config/validate_member`;员工清单在 `agents.yaml`(办公室里增删改会写回)。`dept` 是可选的部门(画布上的部门区,留空 → 「未分配」),老 yaml 缺字段读成空。
- **launcher.py** — `window_title(m)="CCKPT:<name>"`;`launch(m)` 用 `CREATE_NEW_CONSOLE` + `cmd /k title CCKPT:x & cd /d ... & ping -n 4 ... & claude <flags>`(ping 拖 ~3s,给抓句柄留窗口;`--resume` 由办公室右键选会话后经 session_id 传入,不自动)。`launch` 传 `env=child_env()` 剔除 `CLAUDE_CODE_CHILD_SESSION`——cockpit 若从 Claude 会话里启动会继承该标记,透传给员工窗口会让 claude 把自己当嵌套子会话、不保存 transcript(无法 resume)。
- **sessions.py** — 扫 `~/.claude/projects/<编码cwd>/*.jsonl` 列员工历史会话(id/标题/最后活跃)、删除会话;`issue_tag()` 从标题里抠 issue 号(印在桌上那张纸上);标题取最后一条 `ai-title`,回退首条用户消息。
- **winman.py** — Win32(ctypes):`find_by_title / is_window / wait_for_title / is_console_window / bring_to_front / close_window`(「下班」发 WM_CLOSE,不强杀——claude 要有机会落盘 transcript,否则 `--resume` 回不来)。
- **store.py** — `~/.claude/data/claude-cockpit/handles.json`,缓存 `name -> hwnd`,重启 cockpit 复用还活着的窗口。
- **settings.py** — `~/.claude/data/claude-cockpit/settings.json`,办公室的小设置(`sound_enabled` 默认开、`always_on_top`)+ `office` 段(画布布局:部门区位置尺寸 / 工位偏移 / 窗口尺寸 / 缩放,由 `layout.py` 读写);与 store 分开各管各的。
- **sound.py** — `play()` 播自带 `assets/guagua.mp3`(从小青蛙搬来,本项目自带不依赖它),用 `QMediaPlayer`,失败回退 `winsound` 蜂鸣,异常全吞。
- **cc_signals.py** — 文件信号,**两条独立通道**(见下)。
- **matching.py** — `match_pending(records, members)` 按规范化 cwd 把信号对到员工;`norm_path`。
- **peers.py** — 读 `~/.claude/sessions/<pid>.json` 探「同机 Claude 会话」:`read_peers()` 拿 cwd/会话名/忙闲,`match_peers()` 按规范化 cwd 对到员工(复用 `matching.norm_path`,与 `match_pending` 同口径)。判活 = `pid_alive()` + `updatedAt` 30min 时效兜底(pid 会被系统复用,光看 pid 会把陈旧残留当活会话);同一 cwd 多会话取 `updated_at` 最大的那个。**那批 json 是 Claude Code 的内部文件、不是公开契约**,所以本模块只读不写、异常全吞:探不到就返回空,办公室退回兜底的「运行中」,不影响任何既有功能。
- **services.py** — 机房里那几台本地服务(mysql / redis / apache)的清单和探活。纯逻辑不 import Qt。探法一律 **TCP connect**:查进程名会把「进程起来了但端口还没听」报成假绿,查 Windows 服务则漏掉命令行拉起来的实例。**`TIMEOUT=2.5` 别调小**——Windows 连本机一个关掉的端口会先重试 SYN ~2 秒才回「连接被拒」,超时短于这个数,每个停掉的服务都会被误判成「无响应」(琥珀)、永远看不到灭灯(实测 0.3s 全是 TimeoutError);代价由 `probe_all` 的**并发**兜住,一轮的耗时是最慢那台而不是几台相加。清单来自 `services.yaml`(没有就用内置那三个),坏数据退回缺省、绝不抛。**只探不启停**:启停要管理员权限,看板上误点一下就把 MySQL 关了。
- **layout.py** — 办公室画布的布局账本(纯逻辑,不 import Qt):`parse/dump` 读写 settings.json 的 `office` 段、`ensure` 补齐缺省、`dept_of` 归属兜底(空部门 → `UNASSIGNED="未分配"`)。**部门区坐标是绝对值 `[x,y,w,h]`,工位坐标是「相对所属部门区」的偏移 `[x,y]`**(工位是部门区的 Qt 子项,整块挪动时不用重算)。新部门区按人数铺成最多 3 列的网格(不然 13 个员工会被一个个向右撑成一条两千多像素的窄带)。坏数据一律丢弃回默认、绝不抛(与 `peers.py` 同口径:布局是便利功能,不能因为它打不开办公室)。
- **office/theme.py** — 画布的**全部配色**(**家具用低饱和的浅木色 + 深灰蓝椅**,地面和部门区仍是浅灰白;**饱和色只留给屏幕和小人**——家具和状态色不在同一个饱和度档上,所以不抢戏)。**对比度是对着部门区调的,不是对着画布**:工位坐在部门区上,和部门区分不开就白搭(踩过:椅子 `#e8eaee` 和部门区 `#e9ecf1` 几乎同明度,字面意义上消失在部门区里)。每样家具都要「桌面 / 板厚 / 桌腿」三档明度,等距的盒子少一档就看不出是个盒子。**改颜色只改这一个文件**,别再散回图元里;`mix()` 用来给工位地面染状态色。
- **office/seat_item.py** — `SeatItem`:一个工位的 **等距(isometric)**自绘(名字 / 桌子 / 后屏风 / 显示器 / 键盘鼠标 / 音响 / 杯子 / 桌上那叠文件 / 椅子 / 小人)。**家具都摆在一套房间坐标 `(x, y, z)` 里**,由 `_pt()` 统一投影(2:1 等距:横 2px / 竖 1px,斜边压在像素上不糊);改桌子尺寸要跟着调 `ISO_OX/ISO_OY`(投影原点)把内容摆回工位框中间——桌子加长时前角会往右下伸,不挪原点会顶出 `SEAT_H`,`ISO_TOP / ISO_FX / ISO_FY` 三个平面各有一个画笔坐标系,套上就能用普通 `drawRoundedRect` 画斜面、圆角跟着斜——别再逐个图元手调角度(那样凑出来是张「立面图」,生硬)。竖直的东西(气杆)在等距下屏幕上仍是竖直的,那几个直接用屏幕矩形。**等距下一个盒子该露三个面**(顶 + 左前 ISO_FX + 右前 ISO_FY),少画一面就塌成纸片(音响踩过)。**但小人不走切变**:切变后矩形上沿是斜的,左肩会比右肩高一截、人看着驼背——身体只用 `_pt` 定位置,形状用屏幕坐标的圆和圆角矩形画。工位尺寸 `SEAT_W/SEAT_H` **从 `layout.py` import**(布局算账和绘制各持一份必然对不上)。**屏幕色 = 运行状态,有新消息则屏幕在状态色和「掺了一半白」之间半拍一闪**——**只闪屏幕,不罩整张工位**:整张闪太吵(一屋子里有两三个在等你,画面就有两三大块在呼吸),而屏幕是这张图上唯一的亮色块、又已经放大了一倍,闪它够跳;「状态」和「在等你」落在同一个物件上也不打架。屏幕上那几行「字」在 `忙碌中` 时**往上滚**(`advance_scroll`,像终端在刷输出):宽度表按员工名错开一个起点,免得一屋子屏幕整齐划一地同步滚;滚出上沿的那行要 `setClipRect` 裁掉(裁剪跟着当前变换走,裁出来正好是贴合屏幕的平行四边形),不然会画到边框和桌面上。导出 `UP_STATES`(起来了的那几个状态,手型/闪烁/朗读统一按它判断,别再散着写 `== "running"`)。`paint` 里另有 `present = state != "down"`,和 `up` **不是一回事**:`启动中` 是「人到了但还没坐下」,屏幕该亮琥珀、旁边该站着人,只有真没上班才是空椅子 + 黑屏 + 整张置灰。**员工名印在后屏风上、右对齐**(贴着屏风右端,像工位上的名牌;不再浮在工位顶上,也别顶着显示器那头):① 名牌用 `ISO_TEXT_FX` 而不是 `ISO_FX`——后者把字横向拉成两倍宽;② 按**字宽**裁(`_plate_text` 用 QFontMetricsF),按字数裁不行:`etl` 和 `customer-web` 宽度差三倍;③ 可用长度 `PLATE_LEN` 是**算出来的**、别写死:`(DESK_X - PLATE_X) * 2.2361`——两头卡死(左边界是显示器右沿 `MON_X+MON_W`、右边界是桌子右端),所以改桌长或显示器宽度,名牌自动跟着变。两个坑:名牌在 `y=1.4` 的屏风面上、显示器在 `y=6` 的面上,**差 4.6 个单位才对齐到同一条屏幕竖线**(屏幕 x = `OX+(x-y)*2`),接边时要减掉;长度是沿板方向的真实长度,横向差 1 单位 = 2px、沿板方向是 `2/0.8944=2.2361`px。现在 81px,最长的 `customer-web`(79px)刚好装得下。④ **显示器一放大,名牌就跟着变短**(左边界就是显示器右沿):现在 `MON_W=24` 对应 70px,`customer-web`(79px)会省略;要保住全名只能收窄显示器或把桌子再加长 3 个单位。⑤ 名牌紧跟屏风画,不能放到 `paint` 最后:文件堆比屏风离镜头近,画在后面会盖住字(所以文件堆也往近侧挪了一格)。**曾经把名牌放在桌下挡板上,撤了**——挡板左侧大半被椅子挡着,还得把抽屉柜挪到左端才腾得出位置,得不偿失。命中区有语义:`r_person`(人和椅子)= 上下班、`r_files`(桌上那叠文件)= 会话历史、`r_speaker`(桌上那个小音响)= 停朗读、`r_rack`(**只有运维那张工位有**,桌上那台小机柜)= 服务地址,其余当「点工位」。**`hit` 和绘制都走 `has_papers()` / `has_rack()` 这两个判断**,别各写一份 `if self._papers`。工位还会 `say()`:冒个气泡说一句(气泡比工位框高,`boundingRect` 要跟着让出那一块,不然被裁掉半截)。`paint` 和 `hit` 共用同一份 `r_*`,**三块互不重叠**(叠了就会「点鼠标弹出会话菜单」这种串台)。**文件堆别画小**:它是右键「接着这条继续 / 删除这条记录」的唯一入口,画小了点不着。现在单张 `PAPER_W × PAPER_H` = 17×15 单位,尺寸是被「最上面那张要印得下 6 位 issue 号」倒推的:字走 `ISO_TEXT_TOP`(直接用 `ISO_TOP` 会把字横竖都拉成 2.236 倍),沿纸的 x 方向 1 单位 = 2.2361px,6 位数字在真机上要 30px。**别拿 QFontMetrics 去卡这条**——离屏平台的回退字体比真机宽一大截(同一串数字 48px vs 30px),按它算会把纸撑到桌子那么大;用例卡的是几何预算 `PAPER_LEN`。一叠里每张错开 `PAPER_STEP`,纸放大后 0.6 个单位看不出是「一叠」。命中区 `r_files` **由这几个常量算出来**(四角取 min/max),纸一改尺寸自动跟着变。issue 号来自 `sessions.issue_tag()`:先认 `#1085`,再退回「独立的一串 3~6 位数字」并躲开日期,认不出就留白——标题里到处是数字,印个错号还不如空着。**`set_papers_enabled(False)` 关掉整叠纸**:运维那张工位用它——那叠纸的意思是「几条历史会话、右键挑一条接着聊」,他不走这条路,腾出来的「显示器右边」正好给桌上的机柜(两样因此永不共存,命中区也就不会串台)。字体是模块级常量:`paint` 是「每个工位 × 每次 tick/闪烁/悬停」都跑的,每帧新建 `QFont` 要走字体匹配查找。`moved` 只在**真拖动过**才发(否则点一下按钮就写一次盘)。**踩过**:`self._hover = False` 在 `__init__` 和 `hoverLeaveEvent` 里各有一处,一次全局替换把新加的状态初始化塞进了 `hoverLeaveEvent`——鼠标一移开就把服务清单清空,桌上的机柜当场消失。往 `__init__` 加字段时认准那一处。
- **office/iso.py** — 等距投影:房间坐标 `(x,y,z)` → 屏幕坐标(`pt/quad/on` + 四个平面的画笔坐标系 + `ISO_OX/OY`、`DESK_X/Y`)。**单独一个模块**是因为 `person.py` 也要用(坐着的人得画在和椅背同一个等距面上),而 person 又被 seat_item 用——留在 seat_item 里就是循环 import。
- **office/person.py** — 小人的两种画法(坐姿 / 站姿),工位和送信的小人**共用这一份**。**坐姿的躯干画在 `ISO_FX` 面上**(和椅背同一个面):两者上沿平行,露出来的肩膀是一条等宽的带子;躯干若用屏幕矩形、椅背用等距面,肩膀会左薄右厚、像人歪在椅子上(踩过)。**站姿仍用屏幕坐标**——它旁边没有要对齐的东西,而切变一个整身会让人看着驼背。头是球,没朝向问题,两种姿势都画圆。**手脚一律用圆头线段**(`RoundCap`),别用细长的圆角矩形——这个尺寸下会糊成毛刺、两头还接不到身上(踩过)。坐姿的手要落在**键盘上**(不凭空停在半路),胳膊起点压得比头低(不然像从脑袋里长出来),而且**肘部要折一下**——肩到手拉一根直线,人看着像插了两根棍子;肘的偏移直接加在**屏幕坐标**上(等距坐标里没有「屏幕往下」这个方向,换算反而绕)；**腿必须画在椅子之前**(`draw_sitting_legs` 单独一个函数,由 seat_item 在画椅子前调用):腿在座垫和五爪底盘后面,画在后面会盖住它们,看着像人把腿甩到椅子外面(踩过)。桌下没挡板,所以只有膝盖和小腿那一截露在桌子底下,刚好。
- **office/bubble.py** — 说话气泡(折行 + 画),**送信的小人和工位共用这一份**。**按字宽折行、不按字数**(中文一个字是英文两倍宽),而且**不把英文词从中间劈开**——按字符折的话 `ETL` 会排成「ET / L」,一眼看出是机器折的;最多 3 行,末行省略号。底色用**纸白**不用地毯色:气泡多半压在地毯或桌面上,同色的话只剩一圈描边撑着。`height()` 给调用方算包围盒用——写死的话长气泡会被裁掉一块。
- **office/walker_item.py** — `WalkerItem`:会话间发消息时,从发送方工位走到接收方工位、**把那句话显示在气泡里**、再走回去的小人。气泡按正文算大小(`wrap` 折行 → `_bw/_bh` → `boundingRect` 也跟着算,写死的话长气泡会被裁掉一块);**按字宽折行、不按字数**(中文一个字是英文两倍宽),而且**不把英文词从中间劈开**——按字符折的话 `ETL` 会排成「ET / L」,一眼看出是机器折的;最多 3 行,末行省略号。**停留时间按字数算**:一句「好了」和一段两行的说明给同样的时间,要么干等、要么根本没读完。气泡底色用**纸白**不用地毯色——它多半压在地毯上,同色的话只剩一圈描边撑着。没有正文(空消息)时退回原来那三个点。**自带定时器、走完自己从场景里摘掉**(不挂进 `OfficeWindow` 的 tick——那条主循环已经管着轮询/闪烁/音浪三件事);**单程时长固定、不按距离算**(同部门隔 200px、跨部门隔两千多,按速度走远的那趟十几秒早没人看了);**走折线不走直线**——直连会从桌面上横穿过去,路线由 `OfficeWindow._walk_path` 排:退到本工位前的过道 → 沿过道横着走 → 拐进对方工位(工位下半截是空地,所以从椅子往下走一定不穿桌子);取点按**路程**比例、不按段数,不然长段飞快短段磨蹭。
- **office/dept_area.py** — `DeptAreaItem`:部门区(圆角矩形 + 虚线边 + 部门名 + 右下角拉伸角)。整块可拖,工位作为子项跟着走(所以工位**不能**带 `ItemIsSelectable`:Qt 会把选中的可移动图元跟着一起拖,点过的那个工位会多挪一倍——踩过);抓拉伸角时只改尺寸不挪位置,收缩下限 = `max(layout.AREA_MIN, 装得下现有工位)`。
- **office/rack_item.py** — 运维**桌上那台小机柜**(本地服务的状态灯)。**不是独立图元,是工位上的一样家具**——摆在显示器右边,和音响、杯子、那叠文件一个级别;这里只导出画法(`draw`)、命中区(`hit_rect`)和几个纯函数(`report / all_green / any_stuck / tooltip`),由 `seat_item` 在运维那张工位上调用。曾经是占一整格的落地大机柜(还配了「运维走过去看一眼」的动画),搬到桌上之后那趟路就没意义了:柜子就在他手边。**柜门开在 `y=RY` 那个面**(`ISO_FX` 平面:横轴走房间的 x、纵轴是屏幕往下;挂错成常 x 的面,整排灯会飞到柜子外面去——踩过)。**柜门上印不下服务名**(柜门那一条的字高是拿 z 量的、1 单位 = 1px,7pt 的字要 9 个单位,腾这么一条就得吃掉一层槽位),哪一层是谁只在悬停提示里说。尺寸 = **办公桌右半边那块桌面**(显示器右边到桌子右端,连桌子的整个进深)——小了就是个不起眼的盒子,看不出是机柜。**这个尺寸下屏风上那块名牌保不住**:柜子进深占满桌子,它的左下轮廓会整个扫过屏风(等距下 y 变大 = 往左前走),名牌缩到哪儿都被盖掉(试过把它的右边界卡到柜子的 x 上,柜子**顶面**的左角比那还靠左,照样盖)。所以有机柜的工位**跳过屏风上那块名牌**,名字改印在**柜顶**上(像机箱上贴的标签),而且**要画在槽位之后**——先画会被那几条横板盖掉(踩过)。
- **office/view.py** — `OfficeWindow`:场景装配、按部门落座、Ctrl+滚轮缩放、**右键菜单(全部操作的唯一入口:启动 / 续接会话 / 删会话记录 / 复制地址 / 打开目录 / 编辑 / 删除)**、布局存盘(400ms 防抖 + `closeEvent` 兜底)、`refit_scene`(sceneRect 只比内容大一圈窄边:留一大片空地会让东西越拖越散、最后一眼看不全)、`fit_content` / `_fit_if_needed`(缩放到刚好看全;**只在装不下时**才自动缩,还装得下就别动镜头——否则拖完一个工位镜头就自己跳)。**方法名和信号名与退休的 `panel.Panel` 完全一致**,所以 `main.py` 只需换构造类;`set_order` 是空操作(位置由用户摆,排序无意义)。右键菜单由 `build_menu()` 单独搭出来(不在 `contextMenuEvent` 里现搭——`exec` 阻塞,不抽出来没法单测)。`set_always_on_top` 有守卫:值没变就 return、只有本来可见才 `show()`(无条件 show 会把托盘里隐藏着的窗口硬弹出来)。
- **tray_popup.py** — `TrayPopup`:托盘悬停时弹出的无边框小浮层,列出有消息的员工。原住在 `panel.py`,卡片列表退休时搬出来单过。
- **assets.py** — 自带资源路径(`ICON_PATH`)。图标既给托盘也给窗口用,不该继续挂在某个具体界面模块下面。
- **hooks/** — `turn_ended.py`(Stop 写)、`clear.py`(UserPromptSubmit 清)。
- **main.py** — 装配:配置/办公室(`OfficeWindow`)/轮询(1s tick + 200ms 启动轮询 + 550ms 托盘闪 & 工位屏幕闪)/窗口管理/托盘/单实例。

## 信号双通道(关键设计,别搞混)

两条通道都在 `~/.claude/data/claude-cockpit/` 下,**都由本项目自己的 hook 读写**(已和小青蛙解耦):

| 通道 | 目录 | 写 | 清 | 谁读 |
|---|---|---|---|---|
| 权限确认 | `~/.claude/data/claude-cockpit/pending/` | Notification hook(消息含 "permission") | Stop / UserPromptSubmit | **只有办公室** |
| 答完一轮 | `~/.claude/data/claude-cockpit/turn-ended/` | Stop hook | UserPromptSubmit / 点卡已读 / 超时 prune | **只有办公室** |
| 会话间发消息 | `~/.claude/data/claude-cockpit/messages/` | PostToolUse hook(matcher `SendMessage`) | 办公室**读一次删一次** | **只有办公室** |

> 语义各不同:pending=在等你确认权限(Notification 写),turn-ended=答完该你看了(Stop 写);这两条是**状态**,清了才没,办公室都当「有消息」(工位屏幕闪 + 托盘闪 + 提示音)。
> messages 是**事件**:同一个会话可能连发好几条,所以文件名带时间戳+随机后缀(不能像 pending 那样按 session_id 覆盖),读一次删一次,超 30s 的直接丢不补演(办公室没开时攒一堆、开办公室一窝蜂全跑起来没意义)。

`~/.claude/settings.json` 里已挂(全部用 cockpit venv 的 python,**不再引用 desk_buddy**):
- **Stop** → `claude_cockpit.hooks.turn_ended`(写答完 + 顺手清 pending)
- **UserPromptSubmit** → `claude_cockpit.hooks.clear`(清 turn-ended + 清 pending)
- **Notification** → `claude_cockpit.hooks.notify`(消息含 "permission" 时写 pending)
- **PostToolUse**(matcher `SendMessage`)→ `claude_cockpit.hooks.message_sent`(记一笔「谁给谁发了什么」:收件人 + 正文)。**为什么用 hook**:会话间的消息走 `~/.claude/sessions/*.json` 里那个 `messagingSocketPath` 命名管道,那是 Claude Code 的内部协议、不是公开契约(同 peers.py 的口径),不碰;PostToolUse 是官方 hook,发送方必然触发、负载里就有收件人。

## 当前交互行为

- **启动**:右键工位 →「启动(新会话)」或「续接会话 ▸ 某条」。**工位上没有任何按钮**,所有操作都在右键菜单里;已经在跑的员工菜单里没有启动项(防重复启动)。启动是非阻塞的:立刻显示「启动中」,200ms 快轮询**趁 claude 改标题前**抓 HWND 落盘,再转「运行中」。
- **主视图是一张办公室平面图**(不再是竖排卡片列表):可滚动的画布(背景是 60px 的地砖,棋盘式隔一块深一档——纯网格线读起来像方格纸),上面是**部门区**(按 `Member.dept` 分,没填的进「未分配」),部门区上摆着**工位**。工位 200×146(`layout.SEAT_W/SEAT_H`),**等距(isometric)**画法(参考那种等距办公桌插画:浅木桌 + 深灰蓝转椅):菱形桌面 + 两条朝镜头的桌沿板厚,底下是**左端侧板腿 + 右端抽屉柜**、中间整段留空(试过沿近侧长边加通长挡板,撤了——只撤座位那一段更尴尬,整条去掉才干净)、里侧边上立一道**低屏风**(名牌印在它右段)——「一块板 + 四条细腿」画出来就是张**餐桌**,工位的辨识度就在这两块板和那道屏风上(踩过)。显示器立在桌子里侧、**屏幕朝左前正对着座位**(尺寸/位置是 `MON_X/MON_Y/MON_W/MON_H` 四个常量,名牌长度由它们算出来;和后左角的音响之间要留出间距——贴在一起看着是连体的),键盘鼠标在显示器前面(= 坐着那人手的位置),杯子在音响前面,那叠文件**竖放**在显示器右侧;五爪转椅在桌子左前方,小人坐在椅子上。椅背有两条约束:**① 位置**要落在人的左下方(= 房间坐标的 +y,人背后那一侧),一眼能看出椅子朝着屏幕;和人同点会读成「朝着镜头」,偏太多又会读成「人没坐在座位中间」——现在人偏 1.5、椅背偏 4 个单位,底盘和气杆仍在 `(HX, HY)`。**② 形状**必须画在 `ISO_FX` 面上:画面里别的东西都是斜的,椅背要是屏幕坐标的矩形(上沿水平),就像一块正对镜头的板子,读不出朝向(踩过两次:先错在位置、再错在形状)。**「前面」在等距下要 x、y 一起加**——只加 y 是往左前走、看着像「在左边」,x、y 同时加才是屏幕正下方(杯子踩过)。**画面上不写状态也不写会话标题**——状态看屏幕颜色,文字版在悬停提示里(按落点给:停在人身上给状态,停在文件堆上给会话)。人坐桌前、屏幕对着人,朝向才对——把人摆在桌子后面等于让他盯着显示器背面。正俯视画出来所有东西像贴纸,而且显示器只能画背面、认不出是电脑。**饱和色只给两样**:屏幕(=状态)和小人(=员工配色,头/躯干/四肢按同一配色分浅中深三档),家具一律低饱和土色,场景才不花。**桌子是 `DESK_X × DESK_Y` = 57×22 的长方桌(长边对着人),别画成正方形**——办公桌永远是长边对着人,正方形一眼就看出不对;而 34×34 那版 136px 宽,配 46px 高的小人像「会议桌旁站了个玩偶」,近角还伸得太远、右下一大片空。桌上的东西和命中区都按这两个常量排,改尺寸得跟着重排。**音响别摆在人正前方**:小人的头会顶到桌面上来,摆那儿就压在脑袋上、连命中区都和「点人」抢(踩过)。**小人是纯色的、不带 emoji**——emoji 只留给托盘悬停列表;一屋子人时 emoji 挤在 15px 的脑袋上根本认不出。
- **状态看屏幕**:显示器屏幕面按状态上色;**没上班就是空椅子 + 黑屏**(比「整张工位灰掉」直觉得多)——`启动中`(琥珀,还没起来)/ `忙碌中`(蓝,起来了正在干活)/ `空闲`(绿,可以找它了)/ 未运行(灭,整张工位置灰、**椅子还要再单独淡到 0.5 进部门区**——深灰蓝的椅子不淡的话,空工位看着像「人刚离开」而不是「没上班」)。**状态只用屏幕颜色表达,画面上不写字**(文字版挂在工位 tooltip 上,悬停查得到)。**`忙碌中` 的屏幕还会滚**(那几行字往上走,120ms 一帧)——空闲的不滚:一屋子屏幕全在动就没信息量了,而且这是「谁在忙」的第二遍表达(第一遍是屏幕颜色),动起来才互相加强。和音浪同一个口径:**有人在忙才开表**,没人干活就停,不白烧 CPU。**上班了就坐好**:只要不是 `未运行`,小人就坐在椅子上(`启动中` 也坐着——人到了);`未运行` 则连人都没有(空椅子)。曾经做过「空闲就站起来」的两种姿势,砍了——状态已经由屏幕颜色表达,姿势再表一遍是重复信息,而且一屋子人站站坐坐很花。忙/闲来自 `peers` 探到的会话状态;**窗口活着但探不到状态时兜底显示 `运行中`(绿),绝不退化成「未运行」**(否则会重复开空白窗口,见硬约束 3)。
- **有新消息**(答完一轮/等权限):**屏幕闪**(550ms 半拍,在状态色和掺了一半白之间跳;底色仍是状态色,所以「谁在忙」和「谁在叫你」不打架。曾经是整张工位罩光晕,撤了——一屋子里几个人同时等你就有好几大块在呼吸,太吵)+ **托盘图标闪** + **响一声提示音**(员工「新进入」pending 时响一声,首个 tick 静默播种避免开机狂叫;托盘菜单「提示音」可关,存 settings.json)。**未运行的工位一律不闪**——没窗口就没有「在等你」这回事。
- **点工位**(仅运行中):把它的控制台**置前**弹到眼前 + 标记已读 + 停闪。**只置前**——不最大化、也不去最小化其他员工的控制台:那是替用户摆桌面,他自己排好的窗口布局(分屏、并排看两个)会被点一下就打乱,而且每点一个人就要重排一次。`bring_to_front` 里**只有最小化的窗口才 `SW_RESTORE`**:无条件 restore 会把本来最大化的控制台还原成小窗(踩过的反面——之前正因为这个才改用 maximize,现在按需 restore 才是对的解法)。未运行点工位无反应(只有右键「上班」能开)。
- **托盘闪烁** = `cur_pending - acked` 非空才闪;点托盘图标或点任一工位 → ack 停闪(其余工位的屏幕仍在闪,逐个点掉);新员工答完会重新闪。
- **会话之间发消息** → 发送方的小人**起身走到对方工位**、**冒个气泡把那句话说出来**、再走回来坐下(正文由 PostToolUse hook 从 `tool_input.message` 记下来,写入时就压掉换行并截断到 `cc_signals.MSG_MAX`——整篇正文既画不下也没必要落盘)(走的时候自己工位上是空椅子,不然同一个人出现两次)。hook 记的收件人是**会话名**(fad-backend-2-f3),不是员工名(fad-2),要经 `cur_peers` 反查;对不上就安静丢掉——这只是锦上添花的动画,不能因为它影响正事。同时最多 4 个在跑(`OfficeWindow.MAX_WALKERS`),再多就是一屋子人乱窜、反而看不出谁找谁。
- **机房**:办公室里有一块「机房」区(`layout.SERVER_ROOM`),里面是**运维那个固定岗位**;本地服务的那台**小机柜就摆在他桌上、显示器右边**(不是独立图元,是工位上的一样家具)。一层 1U = 一个服务,右端一颗灯:**绿 = 端口听得到、灭 = 没在跑、琥珀半拍一闪 = 端口在但不搭理你**;和工位「屏幕色 = 状态」同一套语义、同一拍闪。桌面尺寸下印不下服务名,**哪一层是谁靠悬停提示**;右键机柜(工位上的 `rack` 命中区)只有「复制连接地址 ▸ 某一层」——不做启停。**运维是一个真员工**(`config.OPS_NAME`):有自己的控制台会话、能上下班、点它置前、有会话历史,工位就是标准 `SeatItem`;只是名字和部门锁死、面板里「编辑/删除」**置灰**(要换目录就去 agents.yaml 改那一行),拖到别的部门区也会被扳回机房。**服务状态一变他就在工位上冒个气泡说一句**(全绿报「N 个服务都正常」,否则点名是哪几个不对——柜灯只说明「有没有事」,是哪一层还得凑近看,那句话才是有用的部分);**不变就不说**:每轮都冒一次,一分钟十二次就成噪音了。没上班也不说话(空椅子上冒气泡太灵异)。探测 **5s 一轮、跑在线程池里**,上一轮没回来就跳过这一轮。
- **朗读中**:桌上那个小音响向**左右两侧**一圈圈扩散琥珀色音浪(越远越淡,180ms 一帧;**只在有人朗读时才开表**,没人说话不空转);点音响 = 停止播放。
- **会话标题不占版面**:悬停在桌上那叠文件上才显示(以及共几条历史会话)。选哪条续接、删哪条,都在文件堆的右键子菜单里(`claude --resume <id>`;「上班(新会话)」则不带)。
- **右键工位按落点分菜单**(点谁就是对谁下命令):右键**人/椅子** = `上班(新会话)` / `上班(接着上次那条)`,已经在跑的则是 `下班(关掉控制台)`(会问一句再关,发 WM_CLOSE 等同点 ×,让 claude 有机会落盘 transcript);右键**桌上那叠文件**(一张纸 = 一条历史会话,最多摞 3 张)= `接着这条继续 ▸` / `删除这条记录 ▸`(在跑的员工没有「接着继续」,防重复启动);右键工位其余地方 = `复制会话地址`(把该员工的会话名塞进剪贴板,就是会话间发消息用的地址;员工名 ≠ 会话名,员工叫 `fad-2`、会话叫 `fad-backend-2-f3`,不给出来对不上)/ 打开目录 / 编辑 / 删除。探不到地址时该项**置灰而不是隐藏**(隐藏用户会以为功能没了),tooltip 说明原因。**右键画布空白处** = 新增员工。图元的 `mousePressEvent` **只处理左键**,右键一律 `ignore()` 交给菜单——不挡的话右键会顺带把控制台弹到眼前(踩过)。菜单里的子菜单必须用 `QMenu(标题, 父菜单)` 显式建、再 `addMenu(子菜单)` 挂上去:直接调父菜单的 `addMenu(标题字符串)`,返回的 QMenu 在 Python 侧没人持有,会被回收掉、点开就报 `Internal C++ object already deleted`(踩过)。
- **布局是用户自己摆的**:工位在部门区上拖动**松手咬到最近的空槽位**(槽位就是自动布局那套行列;别人占着的格子跳过,所以不会两个工位叠一起),部门区拖动**咬到 60px 地砖**(边缘不歪在砖缝中间);部门区整块可拖(名下工位跟着走)、右下角可拉伸——收缩下限是 `max(layout.AREA_MIN, 装得下现有工位)`(工位是拖出来的,可能贴在右下角,光按固定下限会把部门区缩到工位底下)。位置/尺寸/缩放存 `settings.json` 的 `office` 段,400ms 防抖落盘、关窗兜底存一次。**把工位拖到哪块部门区,就属于哪个部门**,松手即写回 `agents.yaml`(也可以在右键「编辑」里填)。画布空白右键能 `新建部门区域`;右键部门区能重命名(上面的人一起改部门)、删除(还有人时置灰,先把人拖走)。空部门区靠 `layout.depts` 那份名单存住,否则「先建区、再拖人」第一步就没了。
- **缩放是每个工位自己的事**:右键工位 → `大小 ▸ 标准 / 小 / 更小`(1.0 / 0.7 / 0.5),存进 `settings.json`。整体缩放已经退休——那是把所有人一起缩,根本没解决「员工多了看不过来」;真正要的是把不常用的单独缩小。
- 办公室窗口**可自由缩放**(不再是固定宽 310);单实例(QLocalServer,再启动只把已有窗口弹前台);托盘可显隐/退出;标题栏明暗跟随画布配色(DWM,现在是浅色)。

## 硬约束(踩坑换来的,务必遵守)

1. **绝不批量/齐发启动 claude** —— 同时拉起多个交互式 claude 会**挤崩共享的 Claude Code daemon**,导致「团灭」(所有窗口一起关)。所以**没有「全部启动」按钮**,只能单个、由用户按节奏启动。
2. **句柄在出生时抓、之后只认句柄**:窗口标题先被设成 `CCKPT:<name>`,claude 起来后会改标题;务必趁改名前用 `wait_for_title` 抓到 HWND 缓存。之后所有「窗口还在吗」一律用 `IsWindow(hwnd)`(配合 `is_console_window`)判断,**不要再查标题**。
3. **自动动作只碰「缓存里且还活着」的句柄,绝不自动 launch** —— 否则会重复开空白窗口(历史 bug)。
4. **信号通道已自给自足**:pending + turn-ended 两条都由本项目 hook 读写,不再依赖 desk-buddy(小青蛙)。改 hook/信号目录时保持 cockpit 自洽,别又把它接回 desk_buddy。
5. 无窗启动用 **pythonw.exe**(普通 python.exe 会留个黑框,关掉它会连带杀死 cockpit)。

## Git

仓库作者 `dingminggg`。提交信息用中文 + 末尾带 `Co-Authored-By: Claude ...`。改完跑一遍 pytest + 离屏装配自检(`QApplication.exec` 打桩成返回 0)再提交。
